session_name: DevOps
environment:
  ANSIBLE_CONFIG: ansible/ansible.cfg
start_directory: /home/devops
windows:
- window_name: ansible
  layout: main-vertical
  panes:
    - ssh gitlab.test
    - nano ansible/install_gitlab.yml
    - ansible all -m ping
- window_name: 2
  layout: main-vertical
  panes:
    - nano /home/devops/cloud-API/arch.json
    - 
    -

